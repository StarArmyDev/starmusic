{
  "compilerOptions": {
    /* Opciones basicas */
    // "incremental": true,                   /* Habilitar compilación incremental */
    "target": "es2018",                       /* Especifique la versión de destino de ECMAScript: 'ES3' (defecto), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2018', 'ES2019' o 'ESNEXT'. */
    "module": "commonjs",                     /* Especificar generación de código de módulo: 'none', 'commonjs', 'amd', 'system', 'umd', 'es2015', o 'ESNext'. */
    // "lib": [],                             /* Especifique los archivos de biblioteca que se incluirán en la compilación. */
    // "allowJs": true,                       /* Permitir que se compilen archivos javascript. */
    // "checkJs": true,                       /* Informe de errores en archivos .js. */
    // "jsx": "preserve",                     /* Especifique la generación de código JSX: 'preserve', 'react-native', or 'react'. */
    "declaration": false,                     /* Genera el archivo '.d.ts' correspondiente. */
    // "declarationMap": true,                /* Genera un mapa fuente para cada archivo '.d.ts' correspondiente. */
    // "sourceMap": true,                     /* Genera el archivo '.map' correspondiente. */
    // "outFile": "./",                       /* Concatenar y emitir salida a un solo archivo. */
    "outDir": "./js",                           /* Redirigir la estructura de salida al directorio. */
    // "rootDir": "./",                       /* Specify the root directory of input files. Use to control the output directory structure with --outDir. */
    // "composite": true,                     /* Enable project compilation */
    // "tsBuildInfoFile": "./",               /* Specify file to store incremental compilation information */
    "removeComments": false,                  /* No emitir comentarios a la salida. */
    // "noEmit": true,                        /* Do not emit outputs. */
    // "importHelpers": true,                 /* Import emit helpers from 'tslib'. */
    // "downlevelIteration": true,            /* Provide full support for iterables in 'for-of', spread, and destructuring when targeting 'ES5' or 'ES3'. */
    // "isolatedModules": true,               /* Transpile each file as a separate module (similar to 'ts.transpileModule'). */

    /* Opciones estrictas de verificación de tipo */
    "strict": true,                           /* Habilite todas las opciones estrictas de verificación de tipo. */
    "noImplicitAny": false,                   /* Generar error en expresiones y declaraciones con un tipo 'any' implícito. */
    // "strictNullChecks": true,              /* Enable strict null checks. */
    // "strictFunctionTypes": true,           /* Enable strict checking of function types. */
    // "strictBindCallApply": true,           /* Enable strict 'bind', 'call', and 'apply' methods on functions. */
    // "strictPropertyInitialization": true,  /* Enable strict checking of property initialization in classes. */
    // "noImplicitThis": true,                /* Raise error on 'this' expressions with an implied 'any' type. */
    // "alwaysStrict": true,                  /* Parse in strict mode and emit "use strict" for each source file. */

    /* Cheques Adicionales */
    // "noUnusedLocals": true,                /* Report errors on unused locals. */
    // "noUnusedParameters": true,            /* Report errors on unused parameters. */
    // "noImplicitReturns": true,             /* Report error when not all code paths in function return a value. */
    // "noFallthroughCasesInSwitch": true,    /* Report errors for fallthrough cases in switch statement. */

    /* Opciones de resolución del módulo */
    "moduleResolution": "node",               /* Especifique la estrategia de resolución del módulo: 'node' (Node.js) o 'classic' (TypeScript pre-1.6). */
    // "baseUrl": "./",                       /* Directorio base para resolver nombres de módulos no absolutos. */
    // "paths": {},                           /* Una serie de entradas que reasignan importaciones a ubicaciones de búsqueda relativas a 'baseUrl'. */
    // "rootDirs": [],                        /* Lista de carpetas raíz cuyo contenido combinado representa la estructura del proyecto en tiempo de ejecución. */
    // "typeRoots": [],                       /* Lista de carpetas para incluir definiciones de tipo. */
    // "types": [],                           /* Escriba los archivos de declaración que se incluirán en la compilación. */
    // "allowSyntheticDefaultImports": true,  /* Permitir importaciones predeterminadas desde módulos sin exportación predeterminada. Esto no afecta la emisión del código, solo la verificación de tipos. */
    "esModuleInterop": true, /* Permite emitir interoperabilidad entre los módulos CommonJS y ES mediante la creación de objetos de espacio de nombres para todas las importaciones. Implica 'allowSyntheticDefaultImports'. */
    // "preserveSymlinks": true,              /* No resuelva la ruta real de los enlaces simbólicos. */
    // "allowUmdGlobalAccess": true,          /* Permitir acceder a los globales de UMD desde los módulos. */

    /* Opciones de mapa de origen */
    // "sourceRoot": "",                      /* Especifique la ubicación donde el depurador debe ubicar los archivos TypeScript en lugar de las ubicaciones de origen. */
    // "mapRoot": "",                         /* Especifique la ubicación donde el depurador debe ubicar los archivos de mapa en lugar de las ubicaciones generadas. */
    // "inlineSourceMap": true,               /* Emitir un solo archivo con mapas de origen en lugar de tener un archivo separado. */
    // "inlineSources": true,                 /* Emitir la fuente junto con los mapas fuente en un solo archivo; requires '--inlineSourceMap' o '--sourceMap' para ajustar. */

    /* Opciones experimentales */
    "experimentalDecorators": true,           /* Permite soporte experimental para decoradores ES7. */
    // "emitDecoratorMetadata": true,         /* Permite soporte experimental para la emisión de metadatos de tipo para decoradores. */

    /* Halo Api */
    "noLib": false,
    "preserveConstEnums": true,
    "suppressImplicitAnyIndexErrors": true
  },
  "include": [
    "./ts/**/*"
  ],
  "exclude": [
    "node_modules"
  ]
}
